spring:
  application:
    name: fcx
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  cloud:
    aws:
      region:
        static: us-east-1
      credentials:
        access-key: teste
        secret-key: teste
      dynamodb:
        endpoint: http://localhost:8000
